import ListItemPresenter from "./ListItemPresenter";

const ListItemContainer = ({ title, content }) => {
  return <ListItemPresenter title={title} content={content} />;
};

export default ListItemContainer;
